package ma.emsi.patientmvc.entities;import jakarta.persistence.*;import jakarta.validation.constraints.DecimalMin;import jakarta.validation.constraints.NotEmpty;import jakarta.validation.constraints.Size;import lombok.AllArgsConstructor;import lombok.Data;import lombok.NoArgsConstructor;import org.springframework.format.annotation.DateTimeFormat;import java.io.Serializable;import java.util.Collection;import java.util.Date;@Entity@Data //génère des méthodes getters, setters, equals(), hashCode() et toString()@NoArgsConstructor //cette annotation génère un constructeur sans argument pour une classe.@AllArgsConstructor //cette annotation génère un constructeur avec des argumentspublic class Patient implements Serializable {//JPA  annociation    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) //ID= mark a field as the primary key of an entity.    private Long id;    @NotEmpty    @Size(min = 4, max = 40)    @Column(length = 50)    private String nom;    @Temporal(TemporalType.DATE)    @DateTimeFormat(pattern = "yyyy-MM-dd")    private Date dateNaissance;    private boolean malade;    @DecimalMin("100")    private int score;    }